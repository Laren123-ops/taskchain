# Week 3｜深度研究包

> 主方向：Dev Tooling / Agent Workflow — 围绕 TaskChain 项目，选3 个标准 / 协议 / SDK写阅读摘要

---

## 研究包：MCP × Chainlink × ERC-7579

---

## 一、MCP（Model Context Protocol）

### 解决什么问题

MCP 是 Animate AI（的前身 Anthropic）提出的标准协议，核心解决：

**AI Agent 如何与外部工具和数据源交互**——在此之前，每个 AI 应用都需要自己实现一套工具调用逻辑，没有统一标准。MCP 提供了：
- **标准化的 Tool 定义格式**：每个 Tool 有 schema（输入/输出），AI 模型可以理解并调用
- **Server/Client 架构**：Tool Provider 充当 Server，AI Agent 充当 Client
- **双向通信**：不仅 AI→Tool，Tool→AI 也可以返回结构化数据

### 边界是什么

| 边界 | 说明 |
|------|------|
| **通信范围** | MCP 设计用于单主机内的 Agent↔ Tool 通信，不适合跨网络/跨链场景 |
| **协议层** | 只定义消息格式和传输层，不处理身份认证、权限控制 |
| **状态管理** | MCP 是无状态的，每次请求独立，不维护会话上下文 |
| **多 Agent 协作** | MCP 原生支持多 Server 连接到一个 Client，但多 Agent（多 Client）协作需要自己实现 |

### 还缺什么

1. **身份与权限**：MCP 没有内置的 Agent 身份认证机制，无法区分「可信 Agent」和「未知 Agent」
2. **跨链支持**：MCP Server 通常运行在单主机，无法直接调用链上合约，需要额外 adapter
3. **状态持久化**：无状态意味着每次任务重启都需要重新初始化上下文，长任务场景需要额外设计
4. **多 Agent 协调**：多个 Agent 之间如何通过 MCP 协作（而非单 Client 多 Server）没有标准答案

---

## 二、Chainlink Functions

### 解决什么问题

Chainlink Functions 是 Chainlink 的「任意代码执行」组件，核心解决：

**链下 AI 计算结果如何可信地写入链上**——传统 Chainlink Data Feeds 是预置数据源，无法满足 AI 场景下「实时计算 + 链上验证」的需求。Functions允许：
- 在 DON（去中心化预言机网络）上运行任意 JavaScript/TypeScript 代码
- 从任何 API 或数据源获取数据并计算
- 将结果通过单一 TX 提交到链上合约
- 多节点聚合结果，避免单点故障

### 边界是什么

| 边界 | 说明 |
|------|------|
| **计算限制** | 每个 Function 请求有 10 秒执行时间和有限 gas预算，不能运行无限循环或大计算 |
| **结果大小** | 返回结果必须可以在链上存储（有限大小），复杂数据需要压缩或哈希中转 |
| **Gas 成本** | 使用 Functions 需要支付 LINK 代币作为费用，成本随计算量和节点数增加 |
| **数据源信任** | 虽然 DON 去中心化，但数据源本身（如中心化 API）仍然是单点依赖 |
| **异步本质** | Function 请求是异步的（先发起请求，等待 DON 执行，再回调），不适合需要即时链上响应的场景 |

### 还缺什么

1. **AI 模型集成**：Functions 目前支持 JavaScript 执行，但没有原生 AI 推理支持（如调用 LLM API 需要自己实现）
2. **跨链数据聚合**：一个 Function 只能向单个链提交结果，跨链数据聚合需要 CCIP 或多 Function协调
3. **隐私计算**：链下执行的所有计算对节点可见，无法实现真正的隐私计算（需要结合 TEE 或 ZK）
4. **结果可验证性**：Functions 返回结果后，合约只能选择相信或拒绝，无法独立验证计算过程的正确性

---

## 三、ERC-7579（Smart Account 标准）

### 解决什么问题

ERC-7579 是以太坊 EIP-4337 的简化版本，核心解决：

**智能合约钱包（Smart Account）的标准化问题**——在 ERC-4337 之前，每个 Smart Account 实现都不同，导致：
- 钱包应用（MetaMask、Rainbow 等）需要为每个实现单独适配
- 开发者每次接入都需要理解不同合约的接口
- 跨钱包的通用插件无法实现

ERC-7579 提出了**Smart Account 的最小标准接口**，让：
- 所有兼容7579 的钱包可以互操作
- 插件（plugin）可以开发一次，部署后被所有兼容钱包使用
- 主人可以无缝切换钱包实现而不丢失配置

### 边界是什么

| 边界 | 说明 |
|------|------|
| **账户抽象范围** | ERC-7579 定义的是「执行层」接口，不包括「支付抽象」（如何让用户用 ERC-20 代币支付 gas） |
| **插件系统** | 7579 的插件接口尚在演进，插件间权限边界和冲突处理没有标准定义 |
| **跨链兼容** | 7579 设计基于以太坊主网 EIP-4337，Layer 2兼容但需要单独适配 |
| **与 Agent 的关系** | 7579 主要面向人类用户钱包，AI Agent 持钱包的场景（谁持有私钥、如何授权）不在讨论范围内 |

### 还缺什么

1. **AI Agent 钱包标准**：没有专门针对「AI Agent 持有和使用 Smart Account」的标准（如预算边界、权限分级、操作审计）
2. **多签集成**：7579 没有内置多签支持，DAO 或团队 Agent 需要额外实现
3. **社交恢复**：虽然 7579 提到了模块化，但具体的社交恢复流程和时间锁没有标准化
4. **插件安全标准**：不同插件可能有冲突权限（如两个插件同时可以转走全部资产），缺乏权限边界标准

---

## 四、三个研究对象对 TaskChain 的综合价值

| 研究对象 | TaskChain 的价值 | 局限性 |
|---------|-----------------|--------|
| **MCP** | Main Agent ↔ Data Agent 的通信标准 | 无跨链支持，需要 adapter |
| **Chainlink Functions** | Week 5+ 替代手动哈希提交，实现链下验证自动化 | 无 AI 原生支持，需要自己接 LLM |
| **ERC-7579** | AI Agent 钱包标准化的未来方向 | 目前面向人类钱包，Agent 钱包标准缺位 |

---

## 五、核心洞察：缺位的 Agent钱包标准

三个研究对象综合来看，**最缺的不是一个新协议，而是「AI Agent 钱包」的完整标准**：

```
理想中的 Agent 钱包标准应该包含：
├── 身份层：Agent 的链上身份（如 ENS 或 DID）
├── 权限层：主人给 Agent 的预算边界（单次 / 单日 /合约白名单）
├── 执行层：Agent 有权签名的交易类型（如只能调用 TaskChain 合约）
├── 审计层：所有操作的不可篡改日志
├── 恢复层：主人可以随时冻结 Agent 钱包或撤销权限
└── 结算层：Gas 费用由 Agent 主人还是 Agent 自己支付？
```

**这个缺位是 TaskChain 项目的核心机会之一**——如果未来要真正实现「AI Agent 自主持有资产并执行任务」，需要一个类似 ERC-7579 的标准来定义 Agent 钱包的接口和行为。

---

*文档版本：v1.0 | 日期：2026/06/08 | 深度研究包*