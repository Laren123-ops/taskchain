# TaskChain — Out of Scope 声明

> Hackathon Week 4 范围管控文档

---

## 为什么要明确"不做什么"？

单人参赛 + 7 天 Hackathon，时间和精力有限。如果不主动砍范围，容易在"试试看"中迷失方向，最后 Demo 跑不出来。

**原则**：先跑通一条链路，其他的以后再说。

---

## 砍掉的功能（Week 4 完全不做）

### 1. 翻译 Agent

**砍掉原因**：
- Week 2 的场景里翻译 Agent 是"锦上添花"，不是核心链路
- Hackathon 期间只演示「数据 Agent」一条链路就足够证明概念
- 翻译 Agent 会在 Week 5+ 串入（届时数据 Agent 链路已稳定）

**替代方案**：Demo 中数据 Agent 直接返回英文数据，不做翻译处理

---

### 2. DAO 治理升级合约

**砍掉原因**：
- DAO 治理涉及代币投票、提案队列、时间锁等复杂逻辑
- 这些和"多 Agent 任务分发 + 链上支付"的核心 Demo关系弱
- Hackathon 期间 TaskChain 合约保持简单，不做可升级设计

**替代方案**：合约保持非升级版本（simple contract），部署一次用到底

---

### 3. ZK 证明验证

**砍掉原因**：
- ZK 电路设计、Trusted Setup、证明生成/验证都是专业门槛
- 目前用 SHA-256 哈希验证足以证明数据一致性
- ZK 可在未来引入，用于"在不暴露数据的前提下证明数据正确"

**替代方案**：用 SHA-256 哈希验证代替 ZK 验证（合约内已实现）

---

## 延后的功能（Week 5+ 再做）

### 4. Chainlink 预言机接入

**延后原因**：
- Hackathon 期间数据由 Agent 手动提交，不依赖预言机喂价
- Chainlink Functions 配置需要额外时间和测试币
- 预言机是"数据源可信上链"的重要组件，Week 5+ 接入

**替代方案**：Data Agent 直接提交哈希，不经过预言机

---

### 5. 动态退款比例（部分退款）

**延后原因**：
- 需要预设"部分交付"的仲裁逻辑（谁来判定部分完成？）
- Hackathon MVP 用"全有或全无"（验证通过全款，失败全额退款）已足够演示核心价值
- 动态退款比例涉及更复杂的合约逻辑和争议机制

**替代方案**：合约只有 `refund()` 全额退款，部分退款 Week 5+ 再设计

---

### 6. 前端仪表盘完整版

**延后原因**：
- Hackathon 核心是合约逻辑和 Agent 协作，前端是锦上添花
- 完整的 Web3 前端（钱包连接、合约交互、事件监听）需要时间打磨
- 评委更关注核心链路是否跑通，不要求炫酷前端

**替代方案**：Day 5 只做一个极简版（任务状态列表），用轮询代替 WebSocket

---

## 改成 Mock / Fallback 的功能

### 7. Agent 认证与信誉分

**Mock方案**：
- 不实现链上 SBT 信誉分系统
- Hackathon 期间假设所有 Agent 都是可信的（Trusted Agent List）
- 信誉分系统在 Week 5+ 通过 SBT 或 ZKP 实现

---

### 8. 多币种支持

**Mock 方案**：
- Hackathon 期间只使用 USDC（Polygon 上生态成熟，Gas 费低）
- 不支持 ETH、其他 ERC-20、复杂代币互换
- USDC 转账失败即宣告任务失败，不做降级处理

---

### 9. 跨链消息传递（IBC）

**Mock 方案**：
- 不实现跨链通信，所有操作在 Polygon Amoy 单链完成
- 跨链场景（其他链的 Agent 协作）在 Week 5+ 研究

---

## Week 4 最终 Scope 清单

```
✅ 真实实现（Hackathon 必须跑通）
├── 智能合约（TaskChain.sol）：锁定 / 哈希验证 / 放款 / 退款
├── MCP 协议：Main Agent ↔ Data Agent 通信
├── 端到端 Demo：锁定 → 验证 → 放款全链路
└── 测试网部署：Polygon Amoy

🟡 Mock / Fallback（可跑通但不完美）
├── 前端仪表盘（轮询代替 WebSocket）
├── Agent 容错（try/catch + 超时重试）
└── 异常场景退款（手动测试）

❌ 砍掉（Week 4 不做）
├── 翻译 Agent
├── DAO 治理升级合约
├── ZK 证明验证
├── Chainlink 预言机
├── 动态退款比例
├──链上 SBT 信誉分
├── 多币种支持
└── 跨链 IBC
```

---

## Scope 控制原则

1. **每新增一个功能，必须砍掉一个现有功能**
2. **Day 5 之后不再引入新功能**，只修 bug
3. **Demo 优先**：任何功能如果不能帮助 Demo 展示核心链路，暂缓

---

*文档版本：v1.0 | 日期：2026/06/08 | Scope 控制文档*